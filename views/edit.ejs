<%- include("partials/header.ejs") %>

<main class="form-container">
    <header class="page-header">
        <h1>Create a New Post</h1>
    </header>

    <!-- The action attribute will tell Express where to send the data -->
    <form action="/update/<%= p.i %>" method="POST" class="editor-form">
        
        <div class="form-group">
            <label for="postTitle">Title</label>
            <input type="text" id="postTitle" name="title" value="<%= p.t %>" required>
        </div>

        <div class="form-group">
            <label for="postContent">Content</label>
            <textarea id="postContent" name="content" rows="12" required><%= p.c %></textarea>
        </div>

        <div class="form-actions">
            <button type="submit" class="btn-primary">Save Changes</button>
            <a href="/" class="btn-cancel">Cancel</a>
        </div>

    </form>
</main>

<%- include("partials/footer.ejs") %>